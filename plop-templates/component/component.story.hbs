import { Story } from '@storybook/react'
import { Meta } from '@storybook/react/types-6-0'
import React from 'react'

import { {{pascalCase compName}}, {{pascalCase compName}}Props } from './{{pascalCase compName}}'

export default {
  component: {{pascalCase compName}},
  title: '{{pascalCase compType}}/{{pascalCase compName}}',
} as Meta

const Template: Story<{{pascalCase compName}}Props> = (args) => <{{pascalCase compName}} {...args} />

export const Simple = Template.bind({})
Simple.args = {}
